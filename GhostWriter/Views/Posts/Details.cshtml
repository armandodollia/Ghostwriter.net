@model Ghostwriter.Entities.Post

<div class="page-header">
    <h1>@Model.Title</h1>
    <p><small>@Model.PostBody</small></p>
</div>

@foreach (var comment in Model.Comments)
{
    @Html.Partial("_Comment", comment)
}